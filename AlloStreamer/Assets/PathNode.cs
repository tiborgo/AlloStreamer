using UnityEngine;
using System.Collections;

public class PathNode : MonoBehaviour {

//		void OnTriggerEnter(Collider other)
//	{
//		if(other.gameObject.tag=="biplane" || other.gameObject.tag=="biplanenotail" || other.gameObject.tag=="plane" || other.gameObject.tag=="planenotail"
//		   || other.gameObject.tag=="ghostplane" || other.gameObject.tag=="ghostplanenotail" || other.gameObject.tag=="privateplane" || other.gameObject.tag=="privateplanenotail" || other.gameObject.tag=="MarineHeli")
//		{
//			
//			if(other.gameObject.tag=="planenotail")
//				Debug.Log ("HIT!!!!!!");
//			
//			Animate anScript = other.gameObject.GetComponent<Animate>();
//			
//			if(anScript.getStartNode() != this.gameObject && anScript.getNoCollide() != this.gameObject)
//			{
//				anScript.setTargetAsNextNode();
//				anScript.setNoCollide(this.gameObject);
//			}
//		}
//		
//	}
}
